<UserControl x:Class="TrackerOfflineSearch.UpdateWizard.Views.ImportResultPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrackerOfflineSearch.UpdateWizard.Views"
             xmlns:vm="clr-namespace:TrackerOfflineSearch.UpdateWizard.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ImportResultPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="{DynamicResource StandardMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="{DynamicResource WizardSectionSeparatorHeight}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="{DynamicResource WizardSectionSeparatorHeight}"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="{Binding Title, FallbackValue='Import completed'}" 
                   Margin="{DynamicResource StandardMargin}" 
                   FontSize="20" 
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding ImportTotal, FallbackValue=0 posts imported, StringFormat='\{0:D\} posts imported '}"
                   HorizontalAlignment="Center"
                   Margin="{DynamicResource StandardMargin}"
                   />
    </Grid>
</UserControl>
