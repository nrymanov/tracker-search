<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:TrackerOfflineSearch.Dialogs.Import"
             mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="450"             
             x:Class="TrackerOfflineSearch.Dialogs.Import.ProgressView"
             x:DataType="vm:ProgressViewModel"
             >

  <Grid RowDefinitions="Auto, *, Auto">
    <!--Header-->
    <Border Grid.Row="0"
            Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
            BorderThickness="0,0,0,1"
            BorderBrush="{DynamicResource SystemControlTransientBorderBrush}"
            Padding="15"
            >
      <TextBlock Classes="wizard-page-title"
                 HorizontalAlignment="Center"
                 Text="Импорт документов"
                 />
    </Border>

    <!--Content-->
    <Grid Grid.Row="1"
          RowDefinitions="Auto, *"
          RowSpacing="5"
          Margin="40"
          >

      <Grid Grid.Row="0" Margin="10" ColumnDefinitions="Auto, *">
        <Svg Width="60" Path="/Assets/import_small.svg"/>

        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Stretch"
                    Spacing="10"
                    Margin="10, 0"
                    >
          <ProgressBar IsIndeterminate="True"/>

          <Grid ColumnDefinitions="*, Auto" ColumnSpacing="5">
            <TextBlock Text="{Binding Message, FallbackValue='Выполняется импорт документов. Пожалуйста подождите...'}"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" Text="{Binding Elapsed, StringFormat='g', FallbackValue=0:00:00}"/>
          </Grid>

        </StackPanel>
      </Grid>

      <Border Grid.Row="1" Classes="info-tip">
        <TextBlock Text="{Binding InfoTip}"/>
      </Border>

    </Grid>

    <!--Buttons-->
    <Border Grid.Row="2"
            Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
            BorderThickness="0,1,0,0"
            BorderBrush="{DynamicResource SystemControlTransientBorderBrush}"
            Padding="5"
            >
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                  Spacing="5"
                  Classes="dlg-footer">
        <Button Content="Отмена"
                IsDefault="True" IsCancel="True"
                Command="{Binding CancelCommand}"
                />
      </StackPanel>
    </Border>
  </Grid>

</UserControl>
