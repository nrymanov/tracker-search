<UserControl x:Class="TrackerOfflineSearch.Views.PostInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:TrackerOfflineSearch.Helpers" 
             xmlns:local="clr-namespace:TrackerOfflineSearch.Views" 
             xmlns:vm="clr-namespace:TrackerOfflineSearch.ViewModels" 
             d:DataContext="{d:DesignInstance Type=vm:PostInfoViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <helpers:FileSizeToStringConverter x:Key="SizeToString"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- General post info -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Margin="3" Text="Title" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBox Grid.Column="1" Grid.Row="0" Margin="3" Text="{Binding Title, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"/>

            <TextBlock Grid.Column="0" Grid.Row="1" Margin="3" Text="Forum" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="3" Text="{Binding ForumName, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"/>

            <TextBlock Grid.Column="0" Grid.Row="2" Margin="3" Text="Size" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBox Grid.Column="1" Grid.Row="2" Margin="3" Text="{Binding Size, Mode=OneWay, Converter={StaticResource SizeToString}}" IsReadOnly="True" BorderThickness="0"/>

            <TextBlock Grid.Column="0" Grid.Row="3" Margin="3" Text="Created" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBox Grid.Column="1" Grid.Row="3" Margin="3" Text="{Binding Created, Mode=OneWay, StringFormat=\{0:d\}}" IsReadOnly="True" BorderThickness="0"/>

            <TextBlock Grid.Column="0" Grid.Row="4" Margin="3" Text="Links" FontWeight="Bold" HorizontalAlignment="Right"/>
            <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal">
                <TextBlock Margin="3" ><Hyperlink Command="{Binding LaunchUrlCommand, Mode=OneWay}" CommandParameter="{Binding Url}"><Run Text="Original post"/></Hyperlink></TextBlock>
                <TextBlock Margin="3" ><Hyperlink Command="{Binding LaunchUrlCommand, Mode=OneWay}" CommandParameter="{Binding TorrentUrl}"><Run Text="Download .torrent"/></Hyperlink></TextBlock>
                <TextBlock Margin="3" ><Hyperlink Command="{Binding LaunchUrlCommand, Mode=OneWay}" CommandParameter="{Binding MagnetUrl}"><Run Text="Magnet link"/></Hyperlink></TextBlock>
            </StackPanel>
            
            <!--<TextBlock Grid.Column="1" Grid.Row="4" Margin="3" Text="{Binding Url}"/>

            <TextBlock Grid.Column="0" Grid.Row="5" Margin="3" Text="Download torrent" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="1" Grid.Row="5" Margin="3" Text="{Binding TorrentUrl}"/>

            <TextBlock Grid.Column="0" Grid.Row="6" Margin="3" Text="Magnet link" FontWeight="Bold" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="1" Grid.Row="6" Margin="3" Text="{Binding MagnetUrl}"/>-->
        </Grid>

        <WebBrowser Grid.Row="1" Margin="3" helpers:WebBrowserBehaviors.BindableSource="{Binding Content, Mode=OneWay}"/>
        <!--<GroupBox Grid.Row="1" Margin="3" Header="Content" >-->
            <!--<TextBox Grid.Row="1" TextWrapping="Wrap" Text="{Binding Content, Mode=OneWay}" IsReadOnly="True"/>-->

            <!--<WebBrowser Grid.Row="1" Margin="3" helpers:WebBrowserBehaviors.BindableSource="{Binding Content, Mode=OneWay}"/>-->
        <!--</GroupBox>-->

    </Grid>
</UserControl>
