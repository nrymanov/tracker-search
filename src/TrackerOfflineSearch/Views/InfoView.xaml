<UserControl x:Class="TrackerOfflineSearch.ViewModels.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrackerOfflineSearch.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="5" Text="Created" FontWeight="Bold"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Margin="0, 5, 5, 5" Text="{Binding CreationDate, FallbackValue=11.11.2022 15:22, StringFormat=\{0:g\}}"/>

        <TextBlock Grid.Column="0" Grid.Row="1" Margin="5, 0, 5, 5" Text="Total" FontWeight="Bold"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Margin="0, 0, 5, 5" Text="{Binding TotalItems, FallbackValue=4224, StringFormat=\{0:N0\} items}"/>
    </Grid>
</UserControl>
