<UserControl x:Class="TrackerOfflineSearch.Views.SearchToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:TrackerOfflineSearch.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" 
             d:DesignWidth="400"
             >
    <UserControl.Resources>
        <Style TargetType="Expander">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ExpandDirection" Value="Down"/>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical">
            <Expander IsExpanded="True" Header="Last queries">
                <!--<Label Content="last queries will be shown here" HorizontalAlignment="Left" VerticalAlignment="Center"/>-->
                <ContentControl prism:RegionManager.RegionName="{x:Static local:RegionNames.HistoryRegion}"/>
            </Expander>

            <Expander IsExpanded="True" Header="Tools" Margin="0, 5, 0, 0">
                <!--<Label Content="Tools"/>-->
                <ContentControl prism:RegionManager.RegionName="{x:Static local:RegionNames.ToolsRegion}"/>
            </Expander>

            <Expander IsExpanded="True" Header="Info" Margin="0, 5, 0, 0">
                <!--<Label Content="Database info" HorizontalAlignment="Left" VerticalAlignment="Center"/>-->
                <ContentControl prism:RegionManager.RegionName="{x:Static local:RegionNames.InfoRegion}"/>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
