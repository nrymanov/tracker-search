<UserControl x:Class="TrackerOfflineSearch.Views.SearchToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:TrackerOfflineSearch.Views" 
             xmlns:vm="clr-namespace:TrackerOfflineSearch.ViewModels" 
             d:DataContext="{d:DesignInstance Type=vm:SearchToolsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="250" 
             d:DesignWidth="400"
             >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="3" Text="Created" FontWeight="Bold"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Margin="3" Text="{Binding CreationDate, FallbackValue=11.11.2022 15:22, StringFormat=\{0:g\}}"/>

        <TextBlock Grid.Column="0" Grid.Row="1" Margin="3" Text="Total" FontWeight="Bold"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Margin="3" Text="{Binding TotalItems, FallbackValue=0, StringFormat=\{0:N0\} items}"/>

        <TextBlock Grid.Column="0" Grid.Row="2" Grid.RowSpan="2" Margin="3" Text="Tools" FontWeight="Bold"/>
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal">
            <TextBlock Margin="3" ><Hyperlink><Run Text="Import"/></Hyperlink></TextBlock>
            <TextBlock Margin="3" ><Hyperlink><Run Text="Optimize"/></Hyperlink></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
