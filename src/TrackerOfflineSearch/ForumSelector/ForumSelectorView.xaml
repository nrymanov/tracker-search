<UserControl x:Class="TrackerOfflineSearch.ForumSelector.ForumSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:TrackerOfflineSearch.ForumSelector"
             xmlns:vm="clr-namespace:TrackerOfflineSearch.ForumSelector"
             d:DataContext="{d:DesignInstance Type=vm:ForumSelectorViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400"
             >

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="Manual"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid Margin="{DynamicResource StandardMargin}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Margin="{DynamicResource StandardMargin}" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                 />

        <TreeView Grid.Row="1"
                  Margin="{DynamicResource StandardMargin}" 
                  ItemsSource="{Binding Items}" 
                  IsTextSearchEnabled="True"
                  >
            <TreeView.Resources>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                </Style>
            </TreeView.Resources>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok"
                    Style="{DynamicResource StandardButtonStyle}"
                    IsDefault="True" 
                    Command="{Binding SelectCommand, Mode=OneWay}"/>
            <Button Content="Clear"
                    Style="{DynamicResource StandardButtonStyle}"
                    Command="{Binding ClearCommand, Mode=OneWay}"/>
            <Button Content="Cancel"
                    Style="{DynamicResource StandardButtonStyle}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</UserControl>
